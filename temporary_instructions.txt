-install
-delete cube
-import protein
-set camera using ctrl+alt+numpad0
-reposition lamp (midway between camera and object)
-switch to Blender Render
-set world horizon to black
-set world ambient occlusion to 0.2
-set lamp data energy to 10
-save blender file
-set output directory
-set file format to AVI JPEG
-set end frame number to 180

FOR EVERY ATOM/STICK:
-go to first frame and insert keyframe -> rotation (XYZ goes green)
-go to last frame and change object z rotation to 358
-insert keyframe -> rotation

-change screen layout to animation
FOR EVERY ATOM/STICK:
-click Channel > Extrapolation Mode > Linear Extrapolation

-go to render and click on 'Animation'

OPTIONAL:
-convert avi to gif
--conda install -c conda-forge imageio
--conda insatll ffmpeg -c conda-forge
--run script